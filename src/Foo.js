/**
 * Returns the string "Hello World!"
 * @returns The string "Hello World!"
 */
const helloWorld = () => {
  return "Hello World!"
};

/**
 * Adds two numbers together
 * @param {number} x 
 * @param {number} y 
 * @returns 
 */
const add = (x,y) => {
    return x + y;
}

export { helloWorld, add };